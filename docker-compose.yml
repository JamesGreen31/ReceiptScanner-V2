services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./processed:/app/processed
      - ./ocr_logs:/app/ocr_logs
      - ./instance:/app/instance
